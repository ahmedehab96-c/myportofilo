{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Flutter: prepare (pub get + gen-l10n)",
      "type": "shell",
      "command": "./prepare_project.sh",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
